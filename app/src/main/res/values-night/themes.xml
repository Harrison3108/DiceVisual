<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DiceVisual" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/dark_primary</item>
        <item name="android:textColor">@color/dark_textColor</item>
        <item name="textFillColor">@color/dark_textColor</item>
        <item name="android:colorBackground">@color/dark_bg</item>
        <item name="android:shadowColor">@color/dark_textShadow</item>
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorSecondary">@color/dark_secondary</item>
        <item name="colorAccent">@color/dark_accent</item>
        <item name="backgroundColor">@color/dark_bgAccent</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>